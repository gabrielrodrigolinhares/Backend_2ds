// Array:são agrupamentos de valores em forma de list
// são idexados a partir de ZERO até o infinito
// podem ter valores homegêneos/heterogêneos
// possuem métodos (funções) próprias

const time = ['petter', 'mariah']
  // indeices   0   1
  console.log(time[1])